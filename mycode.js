
const box = document.createElement('a-box')
box.setAttribute('material', { color: 'red' })
box.setAttribute('position', { x: 0, y: 2, z: 0})
console.log("executing the code")
APP.scene.appendChild(box)

// document.querySelectorAll('[networked-avatar]')